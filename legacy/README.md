# Legacy Viewer

This folder preserves the earlier 2D architecture explorer prototype that previously lived at the repository root.

The live public viewer is now `../index.html`, backed by:

- `../architecture-city-summary.json` for the fast CITY first load
- `../architecture-map.json` for the full safety-filtered graph
- `../source-manifest.json` for source commit and capture metadata

Keep these files here for reference only unless the 2D explorer is intentionally revived.
